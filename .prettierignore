# Raw codeGen output embedded in template literals. This file is the expected value that
# codeGen.test.ts compares against, so reformatting it would defeat the comparison.
# It is generated by `npm run test:before`; the type declarations in staticTypes.ts are
# Prettier-formatted by the generator itself.
jest/assets/staticTypesAsString.ts

# README source. Prettier reflows Markdown, which would change the generated README.md
# without changing its content. Regenerate with `npm run docs:join-assert` after editing.
scripts/readmeStart.md
